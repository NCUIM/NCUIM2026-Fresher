# 本機開發：docker compose up -d 後即可直接使用此連線字串
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ncuim?schema=public"

# 實機測試用的 HTTPS 隧道。到 ngrok.com 註冊免費帳號後，
# 於 Your Authtoken 頁面複製權杖貼在這裡，再執行 npm run tunnel。
#
# 為什麼需要：瀏覽器只在安全來源允許使用相機。手機連 http://192.168.x.x:3000
# 不算安全來源，掃描功能會直接被拒——見 ADR-0004。
NGROK_AUTHTOKEN=""

# 隧道要指向哪個埠。npm run tunnel 指 3000、tunnel:test 指 3001，
# 直接跑 docker compose 時才需要自己設。
# TUNNEL_PORT="3000"

# Admin 帳號種子（僅供本機建立初始管理員使用）
SEED_ADMIN_USERNAME="admin"
SEED_ADMIN_PASSWORD="change-me"

# 對外網址。寄出去的驗證信與找回連結會用它組出網址。
#
# ⚠️ 正式環境必須設定。沒設定時只能相信請求標頭，而標頭是客戶端可控的——
#    有人把 Host 換成自己的網域，找回連結就會寄到攻擊者手上。
#    開發時不設也能運作（會自動從標頭推導，隧道網址也正確）。
# PUBLIC_ORIGIN="https://your-domain.example"

# 寄信設定。未設定 SMTP_HOST 時，信件內容只會印到伺服器紀錄，不會真的寄出。
#
# ⚠️ 正式環境必須設定，否則找回身分的信不會送達，
#    而 ADR-0001 所述的十四天查看期就等同不存在。
#
# 用 Gmail 寄的話填以下這組（填完執行 npm run mail:test -- 你的信箱 驗證）：
#
#   SMTP_PASSWORD 不是你的 Google 登入密碼，而是「應用程式密碼」：
#     1. 帳戶必須先開啟兩步驟驗證，否則看不到這個功能
#     2. 到 https://myaccount.google.com/apppasswords 產生 16 碼密碼
#
#   SMTP_FROM 的信箱要與 SMTP_USER 相同。Gmail 會改寫或退回不一致的寄件者，
#   結果是信看似寄出、實際被擋。
#
#   免費 Gmail 帳號每天約 500 封上限。七十人的活動用不到，
#   但每次重寄驗證信都算一封，測試時不要無限重試。
#
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="you@gmail.com"
# SMTP_PASSWORD="十六碼應用程式密碼"
# SMTP_FROM="NCUIM 新生茶會 <you@gmail.com>"
#
# 埠與加密必須配對：587 搭 SMTP_SECURE="false"（STARTTLS）、
# 465 搭 SMTP_SECURE="true"（SSL）。交叉設定會卡住直到連線逾時。

# ── 用自己的網域寄（交易型寄信服務） ─────────────────────────
#
# 寄件者是 noreply@你的網域 而不是某個 gmail.com。做法是在 DNS 加上
# SPF 與 DKIM 記錄完成網域驗證，服務商才允許你用那個網域當寄件者。
#
# ⚠️ 網域驗證解決的是「信會不會被丟進垃圾桶」，**不是**認證方式。
#    驗證完成後仍然需要一組 API 金鑰當 SMTP 密碼。
#
# 三家都只是換 host 與帳密，程式一個字都不用改：
#
#   Resend       smtp.resend.com     使用者固定填 "resend"，密碼是 re_ 開頭的金鑰
#   SendGrid     smtp.sendgrid.net   使用者固定填 "apikey"，密碼是 SG. 開頭的金鑰
#   Amazon SES   email-smtp.<區域>.amazonaws.com   使用者與密碼是 SES 專用的 SMTP 憑證
#                                    （不是 AWS 的 access key，要另外產生）
#
# SMTP_HOST="smtp.resend.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="resend"
# SMTP_PASSWORD="re_xxxxxxxxxxxx"
# SMTP_FROM="NCUIM 新生茶會 <noreply@你的網域>"
#
# SMTP_FROM 的網域必須是已經驗證過的那一個。用未驗證的網域寄，
# 服務商會直接拒收——症狀與帳密錯誤不同，錯誤訊息會提到 domain 或 sender。
#
# 走這條路時 PUBLIC_ORIGIN 幾乎一定要一起設：信裡的連結若還指向
# localhost，收信的人點了打不開，而那正是找回身分唯一的入口。

# ── 中文網域（IDN）要注意的事 ──────────────────────────────
#
# 網域含中文字元時（例如 卡拉kara.台灣），SMTP 與 DNS 一律只認 Punycode：
#
#   卡拉kara.台灣  →  xn--kara-9u0g392f.xn--kpry57d
#
# 三件事：
#
#   1. 寄信驗證的是「頂層網域」而不是 www。www.你的網域 是網站的主機名，
#      信箱在 noreply@你的網域（或另開 mail. 子網域）。
#
#   2. SMTP_FROM 兩種寫法都可以，nodemailer 會自己轉成 Punycode。但收信的人
#      在信件地址上看到的一定是 xn-- 那一串——只有顯示名稱（例如
#      「NCUIM 新生茶會」）會照原樣出現。多數信件軟體顯示的是名稱，
#      但點開詳細資料就會看到那串亂碼。
#
#   3. **不是每一家寄信服務都支援 IDN 的網域驗證。** 加 DNS 記錄之前，
#      先到服務商後台試著把網域加進去——加不進去就別浪費時間設 DNS。
#
# 另外，全新的網域沒有寄信信譽，第一批信進垃圾郵件匣的機率不低。
# 而在這個專案裡，那封信就是找回身分的唯一入口（ADR-0001 的十四天查看期）。
# 活動前務必用真的 Gmail 與 Outlook 各收一次，確認不是進垃圾桶。
